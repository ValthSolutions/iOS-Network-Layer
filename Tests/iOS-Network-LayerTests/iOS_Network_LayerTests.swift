import XCTest
@testable import iOS_Network_Layer

final class iOS_Network_LayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(iOS_Network_Layer().text, "Hello, World!")
    }
}
