//
//  File.swift
//  
//
//  Created by LEMIN DAHOVICH on 28.02.2023.
//

/*
 let retryPolicy = RetryPolicy()
 let interceptor = Intercector(adapter: TokenAdapter(setToken: setToken)).retry(retryPolicy)
 let session = MainSessionManager(configuration: URLSessionConfiguration.default, interceptor: interceptor)
 */
